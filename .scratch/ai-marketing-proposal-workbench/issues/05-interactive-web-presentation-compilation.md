# 05 — Interactive Web Presentation Compilation

**What to build:** Compiler Agent transforms ProposalSchema into an independent, responsive HTML presentation adhering to shared design tokens and renders it in a live sandboxed iframe.

**Blocked by:** 04 — Critic Quality Audit & Targeted JSON Patching

**Status:** ready-for-agent

## Acceptance criteria

- [ ] Compiler Agent outputs standalone HTML with shared.css and shared.js linkages
- [ ] Preview iframe renders the generated proposal live without external server dependencies
- [ ] Download HTML button exports the full standalone HTML file
